import 'package:flutter/material.dart';

class VaccineSchedulePage extends StatelessWidget {
  static const routeName = '/vaccine-schedule';

  const VaccineSchedulePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('VACCINE SCHEDULE'),
    );
  }
}
