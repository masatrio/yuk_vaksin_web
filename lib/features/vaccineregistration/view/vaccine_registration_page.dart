import 'package:flutter/material.dart';

class VaccineRegistrationPage extends StatelessWidget {
  static const routeName = '/vaccine-registration';

  const VaccineRegistrationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('VACCINE REGISTRATION'),
    );
  }
}
